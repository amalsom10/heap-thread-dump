Script will generate heap and thread dump of docker containers using marathon API.

Preferable Jump servers to run this script:
Stage: X.X.X.com
Prod: xx.xx.xx.xx

Run the script as below:

python3 dumps.py
